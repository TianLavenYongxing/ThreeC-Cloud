server:
  port: 8090
  servlet:
    context-path: /consumer
spring:
  application:
    name: consumer-service
  cloud:
    nacos:
      server-addr: localhost:30101
      username: nacos
      password: nacos